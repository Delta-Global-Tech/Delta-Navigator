apiVersion: v1
kind: Secret
metadata:
  name: delta-navigator-secrets
  labels:
    app: delta-navigator
type: Opaque
data:
  # Supabase - substitua pelos valores em base64
  # echo -n "sua_url_supabase" | base64
  supabase-url: # SUBSTITUA_AQUI
  # echo -n "sua_chave_supabase" | base64  
  supabase-key: # SUBSTITUA_AQUI
  
  # PostgreSQL - substitua pelos valores em base64
  # echo -n "10.174.1.117" | base64
  postgres-host: MTAuMTc0LjEuMTE3
  # echo -n "5432" | base64
  postgres-port: NTQzMg==
  # echo -n "postgres" | base64
  postgres-db: cG9zdGdyZXM=
  # echo -n "seu_usuario_postgres" | base64
  postgres-user: # SUBSTITUA_AQUI
  # echo -n "sua_senha_postgres" | base64
  postgres-password: # SUBSTITUA_AQUI
  
  # SQL Server - substitua pelos valores em base64
  # echo -n "192.168.10.230" | base64
  sqlserver-host: MTkyLjE2OC4xMC4yMzA=
  # echo -n "49172" | base64
  sqlserver-port: NDkxNzI=
  # echo -n "treynor" | base64
  sqlserver-database: dHJleW5vcg==
  # echo -n "seu_usuario_sqlserver" | base64
  sqlserver-user: # SUBSTITUA_AQUI
  # echo -n "sua_senha_sqlserver" | base64
  sqlserver-password: # SUBSTITUA_AQUI

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: delta-navigator-config
  labels:
    app: delta-navigator
data:
  NODE_ENV: "production"
  API_POSTGRES_PORT: "3002"
  API_SQL_PORT: "3001"
  FRONTEND_PORT: "80"
